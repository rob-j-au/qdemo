apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qdemo.fullname" . }}-test-connection"
  labels:
    {{- include "qdemo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qdemo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
