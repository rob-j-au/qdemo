apiVersion: v1
kind: Service
metadata:
  name: {{ include "qdemo.fullname" . }}
  labels:
    {{- include "qdemo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qdemo.selectorLabels" . | nindent 4 }}
